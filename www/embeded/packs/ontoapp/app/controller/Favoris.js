Ext.define('AM.controller.Favoris', {
    extend: 'Ext.app.Controller',
    stores: [ 'Favoris'],
    models: ['Instance'],
    views: ['favoris.List'],
    init: function() {
       
    }
   
});