(function($) {
    $.fn.imgPreview = function(options) {
    	
    	var defaultSettings = {
    		
    	},
    	settings = $.extend({},defaultSettings,options),
    	$container = $("<div/>");
    	
    	
    }
})(jQuery);

$("a").imgPreview();